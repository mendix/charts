import defaultFilter from "./pages/default.page";

describe("Area chart", () => {
    beforeAll(() => {
        defaultFilter.open();
    });

    describe("when default is configurations are set ", () => {
        it("should render chart", () => {
            //
        });
    });
});
