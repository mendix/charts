import defaultFilter from "./pages/default.page";

describe("Bar chart", () => {
    beforeAll(() => {
        defaultFilter.open();
    });

    it("should generate a chart", () => {
        //
    });

    it("should have two columns with proper classes", () => {
        //
    });

    it("should have legend items with proper classes", () => {
        //
    });

    it("should remove columns when legend items are clicked", () => {
        //
    });
});
