import defaultFilter from "./pages/default.page";

describe("Heat map", () => {
    beforeAll(() => {
        defaultFilter.open();
    });

    it("should generate a chart", () => {
        //
    });
});
