import defaultFilter from "./pages/default.page";

describe("Time series", () => {
    beforeAll(() => {
        defaultFilter.open();
    });

    xit("should generate a chart", () => {
        //
    });
});
