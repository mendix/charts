describe("PlotlyChart", () => {
    xit ("renders the structure correctly", () => {
        //
    });

    xit ("renders the chart", () => {
        //
    });

    xit ("resizes the chart when the parent is resized", () => {
        //
    });
});
